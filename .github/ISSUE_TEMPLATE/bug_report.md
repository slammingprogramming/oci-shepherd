---
name: Bug report
about: Something isn't working as documented
title: ""
labels: bug
---

**Mode** (obtain / monitor / selftest):

**Config** (redact real OCIDs/tenancy details, but keep the structure -
paste your actual `config.yaml` with values swapped for placeholders):

```yaml

```

**Relevant log lines** (from your `log_file`, or `journalctl -u
oci-shepherd-obtain.service` / `oci-shepherd-monitor.service`):

```

```

**Expected behavior:**

**Actual behavior:**

**Environment:**
- OS / distro:
- `oci --version` output:
- Running as root, or a non-root `User=` in the unit file?
